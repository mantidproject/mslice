name: mslice-env
channels:
  - conda-forge
  - mantid
  - mantid/label/nightly
dependencies:
  - coverage
  - ipython
  - mock
  - openssl=1.1.*  # Pinned to the same version as Mantid
  - pip
  - pytest
  - pyqt
  - qtconsole
  - qtpy
